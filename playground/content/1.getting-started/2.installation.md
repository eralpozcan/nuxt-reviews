---
title: Installation
description: Get started with nuxt-reviews in your Nuxt project.
navigation:
  icon: i-lucide-download
---

## Install the Module

::code-group
```bash [pnpm]
pnpm add nuxt-reviews
```

```bash [npm]
npm install nuxt-reviews
```

```bash [yarn]
yarn add nuxt-reviews
```
::

## Add to Nuxt Config

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['nuxt-reviews'],

  reviews: {
    providers: {
      google: {
        apiKey: process.env.GOOGLE_API_KEY,
        placeId: process.env.GOOGLE_PLACE_ID
      }
    }
  }
})
```

That's it! The module automatically registers server API routes and the `useReviews()` composable.

## Environment Variables

Keep API keys out of source code:

```bash [.env]
GOOGLE_API_KEY=AIzaSy...
GOOGLE_PLACE_ID=ChIJ...
```
